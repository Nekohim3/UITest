<Page x:Class="UITest.Views.STSiK"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UITest.Views"
      xmlns:viewModels="clr-namespace:UITest.ViewModels"
      mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:STSiKViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="STSiK">
    <Grid>
        <StackPanel Width="150">
            <TextBlock Text="STSiK" TextAlignment="Center"/>
            <Button Content="Вопросники" Margin="2" Command="{Binding QuestionnairesCmd}"/>
        </StackPanel>
    </Grid>
</Page>
